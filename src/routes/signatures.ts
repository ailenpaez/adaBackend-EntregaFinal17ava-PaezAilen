import { Router } from "express";
import SignatureController from "../controllers/signatures";

const router = Router();


// router.get("/", SignatureController.getAllSignatures);
// router.get("/:id", SignatureController.getSignatureById) // BUSCAR UNA CURSO POR ID
// router.patch("/:id", SignatureController.updatedSignature);
// router.delete('/:id', SignatureController.deleteSignature);
// router.post("/", SignatureController.createSignature)

export default router;

